class Solution {
public:
    int finalValueAfterOperations(vector<string>& o) {
        int ans=0;
        for(auto i:o)
        {
            if(i=="X++"||i=="++X")
                ans++;
            else
                ans--;
        }
        return ans;
    }
};
