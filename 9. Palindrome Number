class Solution {
public:
    bool isPalindrome(int x) {
        if(x<0)return 0;
        long long  c=x;
        long long t=0;
        while(c)
        {
            t=t*10+c%10;
            c/=10;
        }
        return t==x;
    }
};
